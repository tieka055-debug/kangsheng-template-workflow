# 2026-09-20 审计得到的历史风险

原有仓库根目录 Python 文件作为历史实现保留，不是已验证生产入口。本次 Skill 更新没有修复旧构建器，也没有重新验收既有成品。

- 运行 work/ 与 Git 存档的 build_ink.py 不同，v4/v5 规则分叉。
- 固定页边清除与手工 BLD_INK_COVER 会误删 BOM、PCB 注释、尺寸表。
- PART、MATERIAL、HOUSING、TOLERANCE、RoHS 既可能是技术内容，也可能出现在旧标题栏，不能用关键词粗删。
- verify_pair.py 复用清除后的源掩码，且旧函数导入与 v5 不匹配。0.55 覆盖阈值、细组件过滤和单向覆盖不能证明完整。
- build_ink.py 单独入口取 src[0]；调用者须显式处理每一页。
- titleblock_factory.py 固定技术字段并可能截断长型号。
- 历史 DWG QA 62 PASS / 43 FAIL 与生成状态 105 ok 不等价。后续有重做，不把历史 FAIL 数量当作当前坏图数量。
- 旧上传器按 status=ok 发送，无输出哈希绑定的 QA 门禁；部分代码会用单附件或部分成功附件覆盖整记录附件列表。
- 旧 README/HANDOFF 存在历史阶段相互冲突的指令，不能用旧文档授权当前上传。

继续开发时在明确分支和候选输出目录工作。先完成样板与回归，才可宣称替换旧管线。
