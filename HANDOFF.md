> 2026-09-20：以下是历史阶段交接，含已过时、相互冲突的批量/上传规则。当前入口为 `skills/kangsheng-drawings/SKILL.md`。本次 Skill 更新不代表旧管线已修复或成品已验收。

# 新会话交接：逐张重做（质量优先）

## 背景
批量自动版已证明不合格（内容重影/字体错乱/缩放错乱），已全部从飞书产品表回收。
现在的任务：按「原图对照-康生模版检查」表**一张一张**重做，用户逐张验收。

## 权威质量标准（用户认可的3张）
BC-21系列塑高2.25H、BC-21系列塑高1.7H、2D_BC-26系列 2-6P —— 就是手工精做的效果：
矢量管线 + 人工测量分组 + 逐组目检。**新会话必须复刻这个做法，禁止任何自动批量。**

## 工作台
- 工作目录：`/Users/vill/Documents/Codex/2026-09-17/referenced-chatgpt-conversation-this-is-an`
- 飞书 Base `TxgTbNZV8aieOJsT21pcr9jXnPf`
  - 产品表 `tblkasFvHs3hl91c`（**只写「替换图纸」列 fldfi1Wnt8（附件类型），
    绝不碰 2D图纸列 fldesr8bw9**——2D图纸列已恢复为供应商原图，是对照基准）
  - 行序（视图顺序）：1-10=BC-21系列（已认可），11=BC-21沉板1.25，31=BC-26（已认可），
    从 11 开始逐张做（跳过 31 和无 2D 图纸的行，如 12=PCB使用图示）
  - 记录ID↔行对应：按创建顺序拉取（pim_records.ndjson 即视图顺序）
- 冻结资产（work/）：`user_bg_plate.png`（背景底图）、`single_sample_titleblock.png`
  （标题栏母版）、`titleblock_factory.py`（只换图号值）
- 核心脚本：`work/zcode_build_series.py`（矢量构建，--groups-json 传入分组布局）
- 所有 lark-cli 命令用参数列表调用（文件名含括号），`--as user`

## 每张的流程（约5-10分钟/张，不可跳步）
1. 从对照表下载该行「原图(供应商)」PDF（按表顺序，跳过检查状态=已认可的）
2. **定方向**：`set_rotation(90)` 和 `set_rotation(270)` 各渲染一张，肉眼看哪个是正的
3. **测分组**：渲染显示方向+坐标网格图，肉眼读出每个内容组的框；
   用文字锚点（get_text words × rotation_matrix）校准边界
4. **逐组目检**：把每个框从源图裁出来放大看——框内必须恰好是完整的一组内容
   （教训：BC-26 第一次漏了整张剖面图；共享尺寸标注的两个视图必须合并成一个组）
5. **构建**：`python3 work/zcode_build_series.py --src <原图> --out-name <名> \
   --model "<图纸自身图号>" [--set-rot-270] --groups-json <布局.json>`
   （图号规则：用图纸右下角图号栏的原文，如「BC-26-5.0H」）
6. **质检（全过才算完成）**：
   a. 渲染成品 PNG，与原图并排肉眼对比：每组内容都在、位置合理、无重叠、无缺失
   b. 连通域遗漏扫描（源图 vs 成品，除旧图框/水印外零遗漏）
   c. 字号检查：成品里的尺寸标注文字必须与原图等比、清晰
7. **回传**：上传成品到产品表该行「替换图纸」列（fldfi1Wnt8），不动其他列
8. 汇报该行行号+图号，等用户确认后再做下一张（或用户说连续做则继续）

## 避坑清单（每条都是真实事故）
- ❌ 自动连通域分组直接用：框会互相重叠 → 重叠区内容放两遍（重影）
- ❌ 竖版源不转 270：整页内容竖排（67张事故）
- ❌ 内容 insert_image 少 rotate=270：内容转 90°
- ❌ 矩形切分跨组标注：共享尺寸线的两视图撕裂（必须并组）
- ❌ 只验"不缺"不验"好看"：重影/字体问题漏网 → 必须渲染目检
- ❌ 直接覆盖产品表：用户要求先过对照表验收

## 环境依赖（已装好）
python3 + pymupdf + PIL + numpy + pypdf；gh CLI（tieka055-debug）；lark-cli（user 身份）

## 2026-09-19 管线升级 v4（整页 1:1 原位合成）
用户反馈 v3「布局全跑偏」（墨迹包围盒被缩放塞进固定区）。v4 根治：
1. **整页 1:1 原位合成**：不裁剪、不缩放、不重排，墨水层按源图坐标原位叠加到品牌底图。
2. **旧图框自动检测**：源模板为双重图框（分区数字/刻度夹在内外框之间，旧标题栏 A4 格伸进框带）。
   用 lum<225 灰度掩码找每侧最靠内的长线为内容边界，内框以外全部归零；
   另加全局 alpha<0.30 淡墨迹清除（专杀虚线内框/分区数字残迹）。
3. **REV 表（右上角格子）按碰撞取舍**：内容侵入格子区(563.6,28.8)-(820,66.5) → 去格子
   （布局紧张）；干净 → 照常保留。自动判定，无需逐张指定。
4. **避让微缩**：仅当内容放不下（侵入新标题栏区/超出品牌图框）才整页等比微缩，锚定页面原点。
5. 旧标题栏关键词补齐：A4 / 第1页 / 共1页。
- 7 张已重做并回传（沉板1.25、BC-22母座、BC-16 1.8H/2.2H 四款、BC-17系列母座），等用户验收。
- 批量脚本：`rebuild7.py`（重建清单）、`upload7.py`（上传+换「替换图纸」列；
  注意 lark-cli 只允许 cwd//tmp/~/files 下的文件，脚本已 chdir 到输出目录）。
- 后续 ~95 行：用户确认本轮布局后按同管线批量跑。
